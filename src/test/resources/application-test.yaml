testcontainer:
  postgresql:
    image: postgres:14
spring:
  jpa:
    properties:
      hibernate:
        #enable_lazy_load_no_trans: true
        default_schema: demo
    hibernate:
      ddl-auto: create